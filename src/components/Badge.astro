---
interface BadgeProps {
  tone?: "gold" | "olive";
  title?: string;
}

const { tone = "gold", title = "" }: BadgeProps = Astro.props;

const tones = {
  gold: "badge badge-gold",
  olive: "badge badge-olive",
};
---

<span class={`${tones[tone] ?? tones.gold}`}>{title}</span>
