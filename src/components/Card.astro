---
interface Props {
	class?: string;
}
const { class: className = "" } = Astro.props;
---

<div class:list={[
	"flex flex-col gap-6 p-6 md:p-8 bg-secondary rounded-2xl border border-border shadow-card transition-shadow hover:shadow-card-hover",
	className
]}>
	<slot />
</div>
