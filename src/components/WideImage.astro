---
import { Picture } from "astro:assets";
const { image, alt, class: className, loading = "lazy" } = Astro.props;
---

<Picture src={image} alt={alt} loading={loading} class:list={[className, "object-cover object-center w-full"]} />
