---
import Layout from "../layouts/Layout.astro";
import Section from "../components/Section.astro";
import Button from "../components/Button.astro";
---

<Layout pageTitle="Page not found">
  <Section contain={false}>
    <div class="container">
      <div class="mx-auto max-w-3xl rounded-[3rem] bg-white/90 p-12 text-center shadow-[var(--shadow-soft)] ring-1 ring-black/5">
        <span class="text-sm uppercase tracking-[0.35em] text-[color:var(--color-brick)]">404</span>
        <h1 class="mt-6">The slice you’re craving isn’t here</h1>
        <p class="mt-4 text-lg text-[rgba(27,29,33,0.72)]">
          The page you’re looking for may have moved with our latest seasonal refresh. Let’s guide you back to the good stuff.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <Button url="/">Return home</Button>
          <Button variant="secondary" url="/menu">View the menu</Button>
        </div>
      </div>
    </div>
  </Section>
</Layout>
